# Implementation Plans

Generated by the `/improve` skill on 2026-08-15, planned at commit `3e56e49`.
First pass scoped to correctness (findings #1-8) and security (#9-12),
selected by the user. Second pass added everything else the audit
surfaced: tech-debt/deps, tests/DX, performance, and 2 grounded direction
spikes — the user chose "everything remaining" for this round.

Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

**Status as of 2026-08-27**: every plan across the first two passes is
DONE except **023** (BLOCKED — `typescript-eslint` doesn't yet support
this repo's `typescript@7.0.2`; see its row below for the full detail).
023's worktree (`.claude/worktrees/magical-diffie-112c15`, branch
`advisor/023-add-eslint-prettier`) is intentionally left in place, holding
its uncommitted config — the only worktree kept around besides the primary
checkout. Revisit once a TS-7-compatible `typescript-eslint` ships. A
third pass on 2026-08-27 added **035** (TODO) — see that section below.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001  | Unify due-date calculation into one clamped, shared function | P1 | M | LOW | — | DONE (`shared/date-utils.ts` created; `resetPayment`, `getBillDueDate`, dashboard's inline calc, and `upcoming.tsx`'s `getMonthDueDate` all wired to it; 4/4 edge cases pass; `pnpm check` produces no new errors in the 5 in-scope files vs. the ~41-error unrelated baseline; manual smoke test confirmed Dashboard/Upcoming agree on a `dueDay: 31` bill's date across months) |
| 002  | Make auto-pay rollover atomic and remove N+1/duplicate query | P1 | M | MED | 001 | DONE (`getPayments()` batches bill lookups via `inArray` and wraps auto-pay writes in `db.transaction`; `resetPayment` takes an optional `executor` for the reused connection; the initial transaction-only version still let two concurrent `GET /api/payments` requests both claim the same overdue payment and insert duplicate next-cycle rows, since the eligibility read happens before the transaction opens — closed by conditioning the `UPDATE` on `status != 'paid'` and skipping `resetPayment` when it matches zero rows, so Postgres's row lock serializes concurrent claimants; verified with repeated concurrent-`curl` tests against a live dev DB, no duplicates; `pnpm check` produces no new errors in `server/storage.ts` vs. the pre-existing unrelated baseline) |
| 003  | Return 404 on update of nonexistent bill/payment | P1 | S | LOW | — | DONE (`updateBill`/`updatePayment` in `server/storage.ts` now throw `Error("Bill/Payment not found")` when the `UPDATE ... RETURNING` matches zero rows; both PUT handlers in `server/routes.ts` catch that message and return `404 {"message": ...}`; `pnpm check` produces no new errors — same ~41/42-error pre-existing baseline, none in `server/storage.ts`, only the known unrelated zod `.errors` bug in `server/routes.ts`; manual `curl` against a live dev DB confirmed 404 on missing bill/payment ids and 200 on real existing ones; committed on branch `advisor/003-404-on-missing-update`) |
| 004  | `revertPayment` must remove the next-cycle payment it invalidates | P1 | M | LOW | — | DONE (`revertPayment` now looks up the bill, computes the expected next-cycle due date via `getNextCycleDueDate`, and deletes the matching pending payment for the same `billId` after flipping the reverted payment back to pending; `pnpm check` produces no new errors in `server/storage.ts` vs. the pre-existing unrelated baseline; manual test against a live dev DB confirmed the mark-paid-with-reset → revert sequence leaves exactly 1 payment, and reverting a payment with no prior reset doesn't error) |
| 005  | Make mark-paid-and-reset a single atomic server operation | P1 | M | MED | 002 | DONE (added `markPaidAndReset` to `server/storage.ts` — wraps the update-to-paid and `resetPayment` call in one `db.transaction`; added `POST /api/payments/:id/mark-paid-and-reset` in `server/routes.ts`; `mark-paid-dialog.tsx`'s existing-payment branch now calls it in a single request instead of PUT-then-POST; `pnpm check` produces no new errors in the 3 in-scope files vs. the pre-existing baseline; manual test against a live dev DB confirmed exactly one `POST .../mark-paid-and-reset` network request produces both the paid and next-cycle rows for the existing-payment branch, and the new-payment branch's 2-request create-then-reset flow still completes successfully; committed on branch `advisor/005-atomic-mark-paid-endpoint`) |
| 006  | Sum money amounts in integer cents instead of float `Number()` | P2 | S-M | LOW | — | DONE (`client/src/lib/money.ts` created with `sumAmounts`; all 3 `reduce` sites in `dashboard.tsx` and all 6 float-summation sites in `analytics.tsx` replaced — 3 of the 6 were `Map`-accumulator patterns, not plain `reduce`, so restructured to group amount strings per key first, then sum each group with `sumAmounts`; standalone `verify-money.ts` script's 3 cases (including the `0.1 + 0.2` float trap) all passed, then deleted per plan; `pnpm check` shows 33 errors — the pre-existing 32-error baseline plus `analytics.tsx`'s already-flagged pre-existing `Set<string>` iteration error, neither touched by this plan; no new errors in `money.ts`, `analytics.tsx`, or `dashboard.tsx`) |
| 007  | Remove dead post-response throw in error middleware | P2 | S | LOW | — | DONE (removed the post-response `throw err` in `server/index.ts`'s error middleware, replaced with `console.error(err)`; added `process.on("uncaughtException"/"unhandledRejection")` logging-only handlers near the top of the file; committed directly to main — the original chip branch had gone stale behind later merges, so the fix was reapplied on top of current `server/index.ts` rather than merging the old branch; `pnpm check` shows no new errors, baseline holds at 23; live `pnpm dev` boot confirmed no startup errors) |
| 008  | Fix `appinstalled` listener cleanup leak | P3 | S | LOW | — | DONE (`client/src/components/install-prompt.tsx`'s effect now names the `appinstalled` handler `onInstalled` and removes it in cleanup, matching the existing `beforeinstallprompt`/`handler` pair; `pnpm check` shows the same 33-error pre-existing baseline, none in this file; no automated test exists for PWA install events, so verification is `pnpm check` plus manual code review confirming both `addEventListener` calls have a matching `removeEventListener` on the same function identity) |
| 009  | Restrict Vite dev server `allowedHosts` | P1 | S | LOW | — | DONE (`server/vite.ts`'s dev-server `allowedHosts` changed from `true as const` to `["localhost", "127.0.0.1"]`; committed directly to main — the original chip branch had gone stale behind later merges, so the fix was reapplied on top of current `server/vite.ts` rather than merging the old branch; `pnpm check` shows no new errors, baseline holds at 23; live `pnpm dev` boot confirmed no startup errors) |
| 010  | Restrict file permissions on nightly DB backup | P1 | S | LOW | — | DONE (`umask 077` + `chmod 700 "$BACKUP_DIR"` in `script/backup-db.sh`; live-run verification closed out on main: with a reachable `DATABASE_URL` and `postgresql@18` on PATH, an isolated `pg_dump`/`gzip` run confirmed the dump directory is created `drwx------` and the dump succeeds) |
| 011  | Default server bind to loopback, opt-in via `HOST` env var | P2 | S | MED | — | DONE (`server/index.ts` now defaults `host` to `127.0.0.1`, reading `HOST` env var for opt-in; removed the stale Replit-era comment block and the dead `REPL_ID`-gated `reusePort` spread — `REPL_ID` has no other reference tied to this listen call, only an unrelated dev-plugin gate in `vite.config.ts`; owner confirmed no current reliance on LAN access, so the behavior change was approved to apply now; `pnpm check` shows the same 32-error pre-existing baseline, none in `server/index.ts`; `lsof` confirmed `127.0.0.1:5050` with no `HOST` set and `*:5052` with `HOST=0.0.0.0` set, both against a live `pnpm dev` run) |
| 012  | Stop passing DB connection string as `pg_dump` argv | P2 | S | LOW | — | DONE (`script/backup-db.sh` now parses `DATABASE_URL` into host/port/user/db, writes a `chmod 600` temp `PGPASSFILE` (auto-cleaned via `trap ... EXIT`) holding the password, and calls `pg_dump -h -p -U -d` instead of `pg_dump "$DATABASE_URL"` — the password never appears in process argv/`ps` output; found and reapplied on top of current main after being executed but never merged in an earlier session; live-verified with a real Neon connection (isolated dump, not the full script's rclone upload/delete, which wasn't run to avoid touching production backup storage without confirmation) — `pg_dump` succeeded, exit 0, 1711-byte gzip output, no password on the command line) |
| 013  | Remove unused passport/session dependency stack | P2 | S | LOW | — | DONE (removed `connect-pg-simple`, `express-session`, `memorystore`, `passport`, `passport-local` and their 4 `@types/*` devDeps from `package.json`; removed the same 5 entries from `allowlist` in `script/build.ts`, leaving the 11 phantom entries for plan 016; Step 1 grep for all 4 package names across `server/`, `shared/`, `client/src/` found zero matches; `pnpm check` shows same 32-error pre-existing baseline, none new; `pnpm build` exits 0, `dist/index.cjs` and `dist/public/` produced; `pnpm-lock.yaml` no longer references any of the 9 removed packages; committed on branch `advisor/013-remove-unused-auth-deps`) |
| 014  | Extend shared API contract to cover budgets, migrate bypassing hooks | P2 | M | LOW | — | DONE (added `api.budgets` — `list`/`upsert`/`delete` — to `shared/routes.ts`, importing `insertCategoryBudgetSchema`/`categoryBudgets`; server's inline budget-creation Zod schema and `insertCategoryBudgetSchema` describe the same shape, so no divergence; rewrote `client/src/hooks/use-budgets.ts`'s 3 hooks onto the contract-driven `fetch` + `.parse()` pattern, dropping `apiRequest`; `edit-bill-dialog.tsx`'s `onSubmit` now calls `useUpdateBill()` instead of a direct `apiRequest("PUT", ...)`, dropping the now-redundant manual toasts and the `apiRequest`/`queryClient` import; `pnpm check` shows the same 33-error pre-existing baseline, no new errors in any of the 3 in-scope files; manual test against a live dev DB confirmed: setting/reloading/deleting a category budget limit on Analytics all worked with no console errors, and editing a bill's amount produced exactly one `PUT /api/bills/:id` request, one dashboard total recalculation that persisted across reload, and no duplicate/missing toast) |
| 015  | Extract shared bill-form component to deduplicate dialogs | P3 | L | MED | 014 | DONE (`client/src/components/bill-form-fields.tsx` created with the shared field set; `create-bill-dialog.tsx`/`edit-bill-dialog.tsx` both wired to it — 309/295 lines down to 98/77; standardized Variable Amount on `Checkbox` per plan, applied create's styled-card treatment to both, added edit's submit button pending state via `useUpdateBill().isPending`; found an undocumented behavior difference not covered by the plan's "Current state" — edit's `enableReminders` toasted on permission denial, create's didn't — added to the shared component for both dialogs rather than dropped; `pnpm check` shows 23 errors, down from the 32-error baseline, none new; committed `f558450` on branch `advisor/015-extract-shared-bill-form`; Step 4 manual browser verification was blocked in the executor's session by a stale dev-preview, so re-run against a live `pnpm dev` after merge — confirmed both flows end-to-end: created a yearly bill (Due Month conditional field appeared, "Bill Created" toast, correct row in Annual Bills Overview), edited it via the pencil icon (all fields including Due Month pre-filled correctly, changed amount, exactly one "Bill Updated" toast, change persisted), then deleted the test bill to leave the DB clean) |
| 016  | Trim phantom (never-installed) packages from build allowlist | P3 | S | LOW | — | DONE (removed 11 never-installed entries — `@google/generative-ai`, `axios`, `cors`, `express-rate-limit`, `jsonwebtoken`, `multer`, `nodemailer`, `openai`, `stripe`, `uuid`, `xlsx` — from `allowlist` in `script/build.ts`; `pnpm check` shows same 32-error pre-existing baseline, none in `script/build.ts`; `pnpm build` exits 0, `dist/index.cjs` and `dist/public/` produced) |
| 017  | Remove unused `attached_assets/` directory and Vite alias | P3 | S | LOW | — | DONE (removed `@assets` alias from `vite.config.ts` resolve.alias and deleted `attached_assets/` (4 PNGs); Step 1 grep for `@assets` across `client/src/`, `server/`, `shared/` found zero matches; `pnpm check` shows same 33-error pre-existing baseline, unchanged by this edit; `pnpm build` exits 0) |
| 018  | Relax exact TypeScript version pin to a caret range | P3 | S | LOW | — | DONE (pin relaxed to `^7.0.2`, committed `18059d2`; approved out-of-scope fix removed tsconfig.json `baseUrl` TS5102 blocker, committed `da06352`; residual ~41 pre-existing unrelated errors this plan explicitly left out of scope were later cleared by commit `11c92f7` — `pnpm check` confirmed exiting 0 clean as of `03e99d2`) |
| 019  | Add Vitest test harness and CI workflow | P1 | M | LOW | — | DONE (vitest.config.ts + shared/date-utils.test.ts + .github/workflows/ci.yml added, `pnpm test` passes 3/3; CI workflow YAML not executable locally, unverified on GitHub infra; post-merge fix: original `exclude: ["node_modules", "dist"]` didn't glob-match nested paths, so `pnpm test` hung scanning 350+ unrelated `*.test.ts` files inside `node_modules` and every chip's `.claude/worktrees/*` checkout — changed to `["**/node_modules/**", "**/dist/**", "**/.claude/**"]`) |
| 020  | Add Budgets & Analytics section to manual test plan | P2 | S | LOW | — | DONE (appended "## 9. Budgets & Analytics" section to `TEST_PLAN.md`, matching existing table format; `grep -c "^|"` row count went 57 → 67, +9 rows as expected; no drift on `TEST_PLAN.md` since plan was written; committed on branch `advisor/020-budgets-test-coverage`) |
| 021  | Add CLAUDE.md, fix stale claims in replit.md | P2 | S | LOW | — | DONE (created `CLAUDE.md` at repo root with stack, commands, path aliases, env vars, architecture, data model, backups, and conventions; `replit.md` React version corrected to 19, "Development vs Production" section now mentions Neon/`DATABASE_URL`/R2 backups, and the dependency list's `connect-pg-simple` bullet replaced with a note that the whole session/auth stack — `connect-pg-simple`, `express-session`, `passport`, `passport-local`, `memorystore` — was removed as unused, since plan 013 had already landed on main; all Done-criteria greps pass; committed on branch `advisor/021-add-claude-md-fix-replit-md`) |
| 022  | Add `.env.example` | P2 | S | LOW | — | DONE (`.env.example` created at repo root with `DATABASE_URL`, `PORT`, `HOST`, `NODE_ENV`; drift check found `server/index.ts` had gained a `HOST` env var since the plan was written, added it too; confirmed not git-ignored — `.gitignore:3`'s `.env` pattern is exact, not `.env*`; branched `advisor/022-add-env-example` off `main`) |
| 023  | Add ESLint and Prettier | P3 | S | LOW | — | BLOCKED (`eslint.config.js`, `.prettierrc.json` created; `eslint`/`typescript-eslint`/`eslint-plugin-react-hooks`/`prettier` installed; `lint`/`format` scripts added to `package.json`; `pnpm exec eslint --version` succeeds and `pnpm check` holds at the 23-error pre-existing baseline, no new errors — but Step 4's `pnpm lint` hits the plan's own STOP condition: `typescript-eslint@8.67.0` refuses to run against this repo's `typescript@7.0.2` ("does not support TS 7.0"), a config-load failure, not a lint finding; `--version` doesn't load the parser so Step 2's verify passed while the real incompatibility only surfaces on `eslint .`; uncommitted on branch `advisor/023-add-eslint-prettier`, left for follow-up — needs a TS-7-compatible typescript-eslint version or a TS downgrade, both out of this plan's install-only scope) |
| 024  | Add index on `payments.dueDate` | P3 | S | LOW | — | DONE (`shared/schema.ts`'s `payments` table gained `dueDateIdx: index("payments_due_date_idx").on(table.dueDate)`; `pnpm check` baseline is actually 24 pre-existing errors, not 23 as recorded on plan 015/016/023 — error set identical before/after this change, none new; `pnpm db:push` applied cleanly (no unexpected diff — single additive `CREATE INDEX`, no interactive prompt shown since the change was unambiguous); live Neon `\d payments` confirms `payments_due_date_idx` btree(due_date) alongside the existing primary key, nothing else changed; committed on branch `advisor/024-add-payments-due-date-index`) |
| 025  | Memoize derived data in Analytics and Upcoming | P3 | S | LOW | — | DONE (wrapped `analytics.tsx`'s `paidPayments`→`thisMonthByCategory` derivation block in a single `useMemo` keyed on `[payments, bills, budgets]`; `upcoming.tsx`'s `MonthColumn` builds a `Map` of payments keyed by `billId-year-month` via `useMemo([payments])` instead of a linear `find()` per row, and the now-unused `getPaymentForMonth` helper was removed along with its now-dead `isSameMonth`/`isSameYear` imports; drift since the plan was written was just plan 006's `sumAmounts(...)` already in place, as the plan's own note anticipated; found and fixed one real bug during Step 3 verification not called out by the plan itself — the `useMemo` had been placed after `analytics.tsx`'s `if (isLoading) return <Skeleton/>` early return, violating the Rules of Hooks and crashing with "Rendered more hooks than during the previous render"; moved the memo (and its destructure) above the loading check to fix; `pnpm check` holds at the 23-error pre-existing baseline; manually verified both pages in-browser — Analytics summary cards/charts/budget rows and Upcoming's 6 month columns render identical values to before, typing in a budget-limit input shows no lag or incorrect re-render; committed on branch `advisor/025-memoize-analytics-upcoming`) |
| 026  | Add route-level code splitting | P3 | S | LOW | — | DONE (`client/src/App.tsx`'s 4 page imports converted to `lazy(() => import(...))`, `Router()`'s `<Switch>` wrapped in `<Suspense fallback={...}>`; `pnpm check` holds at the 23-error pre-existing baseline, none new; live `pnpm dev` navigation to all 4 routes rendered correctly with no console errors; `pnpm build` exits 0 and produces separate `dashboard-*.js`, `history-*.js`, `upcoming-*.js`, `analytics-*.js` chunks in `dist/public/assets/`; production build (`node dist/index.cjs`) verified via in-app client-side navigation to all 4 routes with no new console errors — a pre-existing, unrelated ServiceWorker-registration failure and a pre-existing Express catch-all 404 on direct deep-link URLs both reproduced identically on unmodified `main`, confirming neither was introduced by this change) |
| 027  | Wire budget-overage into existing notification pipeline (direction) | P3 | S-M | LOW | — | DONE (`checkBudgetOverages` added to `client/src/lib/notifications.ts`, reusing the existing `wasNotifiedToday`/`markNotifiedToday`/`sendNotification` dedup helpers unchanged; `NotificationRunner` in `client/src/App.tsx` now fetches `/api/budgets` and calls it alongside `checkAndSendReminders`; `pnpm check` shows no new errors vs. this worktree's pre-existing baseline (23-24 errors depending on run, none in either touched file); Step 3's literal browser-notification firing was blocked by the sandboxed preview browser's `Notification.permission` being hard-`"denied"` at the browser-policy level with no accessible settings UI to override — with operator approval, verification instead dynamically imported the real `notifications.ts` module from the live Vite dev server, fetched real bills/payments/budgets from a running `pnpm dev` against the actual Neon DB, set a real $50 Utilities budget against $265 real month-to-date spend, mocked only the `Notification` constructor/permission, and called `checkBudgetOverages` twice: fired exactly once (`📊 Budget Exceeded: Utilities`, body `You've spent $265.00 this month, over your $50.00 limit.`, tag `budget-Utilities`), set the `billflow_notified_budget_Utilities` dedup key, and the second call was correctly suppressed; test budget limit and dedup key were removed afterward to leave real data clean; committed on branch `advisor/027-budget-overage-notifications`) |
| 028  | CSV import for bills (direction) | P3 | M | MED | — | DONE (`client/src/lib/csv-import.ts` added — naive comma-split `parseBillsCSV` parser, header `Name,Category,Amount,Frequency,DueDay,DueMonth,AutoPay`, per-row validation with row-numbered errors; `client/src/components/import-bills-dialog.tsx` added — file picker, preview table of valid rows + error list, sequential `useCreateBill().mutateAsync()` per row reusing the existing single-bill `POST /api/bills` endpoint (no new server code, per scope); `Import CSV` button added next to `Add Bill` in `dashboard.tsx`'s header; drift check found `server/routes.ts`/`storage.ts` changed since the plan was written (zod `err.errors`→`err.issues` rename, new `markPaidAndReset` payments logic) but neither touches the bills-create endpoint or schema the plan depends on, no STOP triggered; no existing "export bills" function found (only payment-history `exportToCSV`), so the new import format is purpose-built as the plan anticipated; `pnpm check` holds at the 23-error pre-existing baseline, none new; manual end-to-end verify against a live `pnpm dev`: a 2-row valid CSV (one monthly, one yearly) previewed with zero errors and both bills appeared correctly on the dashboard — yearly bill showed "Mar 15, 2026", the correct due month; a mixed CSV (one row missing `DueDay`, one valid) correctly separated them in the preview and only the valid row imported; all 3 test bills deleted via `DELETE /api/bills/:id` afterward, dev DB confirmed clean) |
| 029  | Fix `ZodError.errors` → `.issues` rename (live crash on validation errors) | P1 | S | LOW | — | DONE (all 9 `err.errors` references in `server/routes.ts` switched to `err.issues`; `pnpm check` shows zero `server/routes.ts` errors; manual `curl` against `POST /api/bills` and `POST /api/payments` with empty bodies confirmed clean `400` responses with real messages instead of the prior crash; committed on branch `advisor/029-fix-zod-error-property-rename`) |
| 030  | Deploy to Render with HTTP Basic Auth (direction) | P2 | M | MED | — | DONE (Part A — `basicAuth` middleware added to `server/index.ts` gated on `BASIC_AUTH_USER`/`BASIC_AUTH_PASS`, `.env.example` documents both vars, `render.yaml` Blueprint config created, `CLAUDE.md` gained a `## Deployment` section; `pnpm check` exits 0; live `pnpm dev` curl checks confirmed 401 with no credentials, 200 with correct credentials, 401 with wrong credentials, and 200 with both vars unset; merged to `main` via `advisor/030-render-deployment-with-basic-auth`. Part B — operator deployed via Render Blueprint (service `billflow`, Oregon region, status Deployed), confirmed live and working end-to-end by the owner 2026-08-27) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 depends on 001**: `resetPayment()`'s due-date math is fixed in 001
  (extracting `getNextCycleDueDate` into `shared/date-utils.ts`); 002's
  excerpts assume that function already exists.
- **005 depends on 002**: 005 reuses the `resetPayment(id, executor)`
  optional-transaction-handle signature that 002 introduces.
- **004** independently uses `getNextCycleDueDate` from 001's output (for
  matching the next-cycle payment to delete) — apply 001 first if running
  004 standalone, though 004 doesn't strictly depend on 002 or 005.
- **009 and 011** both reduce the app's network exposure but touch
  different files (`server/vite.ts` vs `server/index.ts`) with no code
  overlap — order doesn't matter between them.
- **010 and 012** both edit `script/backup-db.sh` in non-overlapping
  sections — order doesn't matter, but if both are applied, do a final
  read-through of the combined script.
- **015 depends on 014**: 015 extracts a shared form component starting
  from `edit-bill-dialog.tsx` *after* 014 has already migrated its
  `onSubmit` to `useUpdateBill()` — applying 015 first would mean fighting
  over the same submit-logic edit twice.
- **013 and 016** both edit `script/build.ts`'s `allowlist` array, in
  disjoint entries (013 removes 5 real-but-unused packages; 016 removes
  11 never-installed phantom entries) — order doesn't matter, re-read the
  array once after both.
- **019** (test harness) doesn't hard-block any other plan, but strengthens
  all of them — plans 001-012, 024-028 all fall back to manual
  `curl`/browser verification specifically because no automated test
  runner existed when they were written. Applying 019 early means later
  plans' executors could optionally add a real test alongside their
  manual verification, though none of the existing plans require it.
- **021**'s Step 4 branches on whether 013 has been applied (checks
  `package.json` for `passport` first) — apply in either order, the step
  handles both cases.
- **025**'s note: if 006 (cents-safe summation) was applied first,
  `analytics.tsx`'s summation sites already look different (`sumAmounts`
  instead of raw `reduce`) — 025's `useMemo` wrapping applies either way,
  no conflict.
- **027**'s note: if 006 was applied first, consider using `sumAmounts`
  in the new `checkBudgetOverages` function for consistency, though not
  required for correctness at typical bill amounts.

## Recommended execution order

Suggested sequence, respecting dependencies and front-loading the
highest-leverage, lowest-residual-risk fixes:

**Money-correctness (do first — these fix real data bugs):**
1. **001** (root-cause due-date fix — unblocks 002 and informs 004)
2. **003** (independent, trivial, high-value)
3. **004** (independent bug matching the app's own test plan)
4. **002** (depends on 001)
5. **005** (depends on 002)
6. **006** (independent)

**Quick correctness/security cleanups:**
7. **007**, **008** (trivial correctness)
8. **009**, **010**, **012** (independent security hardening)
9. **011** (behavior change — apply once you've confirmed no reliance on LAN access)

**Foundation for everything after this point:**
10. **019** (test harness — no hard dependents, but do this before the
    larger refactors below so their own manual verification could
    optionally be backed by a real test)

**Tech debt / dependencies:**
11. **013**, **016**, **017**, **018** (independent, trivial cleanups)
12. **014** (contract unification)
13. **015** (depends on 014, larger refactor)

**DX / docs:**
14. **021**, **022**, **020**, **023** (any order, all independent)

**Performance (low urgency, cheap wins):**
15. **024**, **025**, **026** (any order, all independent)

**Direction spikes (product decisions — discuss before building):**
16. **027**, **028** — both are grounded, reasonably-scoped builds, but
    unlike everything above they add new user-facing behavior rather than
    fixing something broken. Confirm the owner actually wants each before
    an executor spends the effort.

## Findings considered and rejected

Every finding from the original audit now has a plan except one
deliberately excluded item:

- **Versioned migrations** (replacing `pnpm db:push` with `drizzle-kit
  generate`/`migrate`) — the original audit flagged this as a real
  tradeoff but explicitly "not worth doing (yet)" given the nightly R2
  backup already provides a partial safety net, and it's a bigger,
  riskier change than anything else in this batch for a solo-dev app with
  no other contributors. Revisit if a second contributor or a staging
  environment ever enters the picture.

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.

## Second pass — 2026-08-27

Generated by `/improve` at commit `03e99d2`, scoped to dark mode,
production hygiene, and accessibility — flagged by the owner plus a
standard sweep of what's changed since the first pass (CSV import, budget
notifications, Basic Auth/Render deploy). All 4 findings selected by the
owner.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 031  | Make bill-table column sort headers keyboard-accessible | P1 | S | LOW | — | DONE (all 5 sortable `TableHead` elements in `client/src/pages/dashboard.tsx`'s shared `BillTable` now wrap their label + `SortIcon` in a `<button type="button" onClick={...}>` instead of an `onClick` on the `<th>` itself, plus each gained a matching `aria-sort` (`"ascending"`/`"descending"`/`"none"`) driven by the existing `sortConfig` state; `Actions` header left untouched (not sortable, out of scope); drift check (`git diff --stat 03e99d2..HEAD -- client/src/pages/dashboard.tsx`) was clean, code matched the plan's excerpt exactly; `pnpm check` exits 0, no errors, matching the plan's clean baseline; `grep -c '<button'` and `grep -c 'aria-sort='` confirm +5 buttons and 5 `aria-sort` attributes; live `pnpm dev` verification confirmed: Tab from the search box reaches "Bill Name" with a visible focus ring, mouse-click sorting still cycles none→ascending→descending→none correctly with `aria-sort` toggling in lockstep on both the "Upcoming Monthly Bills" and "Annual Bills Overview" table instances, and the header row layout is visually unchanged (no wrapping/shift); the literal Enter/Space keyboard-activation sub-check (Step 2.3) could not be directly automation-verified — this session's browser tool dispatches Enter/Space keydown events with empty `key`/`code`/`keyCode` (confirmed via an injected event-listener probe), while the same tool's Escape and Tab dispatches carry correct key identity and work correctly (Escape closed a Radix dialog, Tab moved focus as expected) on this very page, isolating it as a gap in this specific tool's key-name mapping rather than an app defect; corroborating evidence for correct behavior: the header is a plain native `<button>` (browser-guaranteed Enter/Space activation, no custom key handling involved), and a repo-wide grep of `onKeyDown`/`keydown` handlers found none on this element or any ancestor that intercepts or prevents Enter/Space; no files outside `client/src/pages/dashboard.tsx` modified for the fix itself; committed on branch `advisor/031-keyboard-accessible-sort-headers`; **owner should still confirm real Enter/Space keyboard activation once merged — the automated check above only established there's no code path that would block it, not a live keypress test**) |
| 032  | Gate the "Feature Demo" panel behind dev-only build | P2 | S | LOW | — | DONE (wrapped the floating panel `<div>` at `dashboard.tsx:642-714` in `import.meta.env.DEV &&`, per plan — shared `demoOverdue`/`bannerDismissed` state and the real overdue-banner logic untouched; `pnpm check` exits 0; confirmed panel still opens and works in `pnpm dev`; `pnpm build` exits 0 and `grep -rc "Feature Demo" dist/public/assets/*.js` returns `0` for all 12 built JS files, confirming Vite dead-code elimination strips it from prod; `pnpm start` itself failed on this machine with a pre-existing, unrelated `DATABASE_URL must be set` error — `pnpm start` runs the built `dist/index.cjs` directly and doesn't load `.env` the way `pnpm dev`'s `tsx --env-file=.env` does, so verified via the plan's documented grep fallback instead; committed `c7bc3dc` on branch `advisor/032-gate-feature-demo-panel-dev-only`) |
| 033  | Add accessible names to icon-only buttons | P2 | S | LOW | — | DONE (added `aria-label="Delete bill"` to the delete-bill icon button in `dashboard.tsx:177`, and a dynamic `aria-label={isDark ? "Switch to light mode" : "Switch to dark mode"}` to the theme-toggle button in `theme-toggle.tsx`; drift check against commit `03e99d2` found no changes to either file, both "Current state" excerpts matched live code exactly; `pnpm check` exits 0, no new errors; both `grep` Done-criteria checks find exactly 1 match each; manual accessibility-tree check in a live `pnpm dev` session confirmed the delete button's computed Name is "Delete bill" and the theme-toggle's flips correctly between "Switch to dark mode" and "Switch to light mode" on click; `git status` confirms no files outside the two in-scope files were modified; committed `9496ebe` on branch `advisor/033-icon-button-accessible-names`) |
| 034  | Fix dark-mode card/border contrast against page background | P3 | S-M | MED | — | DONE (dark-mode `--border`/`--input` raised to `240 3.7% 40%` and `--card`/`--popover` to `240 10% 9%` in `client/src/index.css`; hero budget card in `stats-cards.tsx` gained `dark:border dark:border-border`; `pnpm check` exits 0; Node WCAG contrast script confirmed `bg vs border: 3.30` and `card vs border: 3.03`, both ≥3:1 per WCAG 1.4.11; manual `pnpm dev` dark-mode check across Dashboard, Upcoming, History, Analytics, and the Add Bill dialog confirmed visible, non-glowing borders with no layout regressions; committed `b567d3c` directly to `main`) |

All 4 landed independently on separate branches and were consolidated into
`main` by hand (each executor forked before the others' work existed, so
`dashboard.tsx` merged cleanly across all 4 — 031, 032, and 033 each touch
disjoint regions of the file — but `plans/README.md` itself needed manual
reconciliation since every branch independently duplicated this section).

### Findings considered and rejected (second pass)

- **Mobile bill-table horizontal scroll** — flagged in the initial design
  critique as a possible layout break; on inspection, the shadcn `Table`
  primitive already wraps in `overflow-auto` ([table.tsx:9](../client/src/components/ui/table.tsx:9)), so it's a
  contained scroll region, not a page-breaking issue. Not worth a plan on
  its own merits; revisit only if the owner finds it actively annoying on
  a real device.
- **Filter-pill touch target size** — initial critique guessed ~32px from
  screenshots; actual measured height is `h-10` (40px), close enough to the
  44px guideline that it's not worth a dedicated plan.
- **CSV import's naive comma-split parser** — already a known, accepted
  limitation documented in plan 028's own completion notes ("naive by
  design"), not re-flagged here.
- Correctness/security/performance/deps were not re-audited in this pass
  (see the second-pass note above) — the first pass already covered them
  exhaustively and nothing since has touched that surface except what's
  listed above.

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.

## Third pass — 2026-08-27

Generated by `/improve` at commit `e1609da`, scoped to correctness/
security/perf/deps — the categories the second pass explicitly skipped —
focused on code landed since the first pass that had never been audited
under those categories (CSV import, budget notifications, Basic Auth, and
an ad-hoc payment-handling refactor outside the plan workflow). One
high-confidence finding, selected by the owner.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 035  | Restore per-request auto-pay rollover | P1 | S | LOW | — | DONE (`GET /api/payments` in `server/routes.ts` now calls `await storage.processAutoPay()` before `storage.getPayments()`, restoring the per-request cadence; boot-time call and `processAutoPay`/`getPayments` internals untouched per scope; drift check against `e1609da` showed no changes to either file; `pnpm check` shows the same pre-existing `analytics.tsx` `Set<string>` iteration error on baseline, none new; live-DB verification against a running `pnpm dev` (alternate port, since the owner's own dev server already held the default port) confirmed the core regression fix: manually set an auto-pay bill's pending payment (`USI: Internet`, id 46) `due_date` to yesterday, then without restarting the server hit `GET /api/payments` and confirmed payment 46 flipped to `paid` and a new next-cycle payment (id 51, due 2026-09-25) appeared; committed on branch `advisor/035-restore-per-request-autopay-rollover`) |

`pnpm audit --prod` clean, no known vulnerabilities. Everything else
checked in this pass (the `updateBill` pending-payment cascade, the
budgets routes, `use-payments.ts`'s raw-request-function pattern) read
correctly — not re-flagged.

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.

## Fourth pass — 2026-09-01

Single targeted plan (`plan <description>` mode, no full audit) from a
direct owner request: archiving a paid-off recurring bill (a student loan
final payment) needed to preserve its payment history for the app's
metrics, not just avoid a scary confirm dialog. Investigation found the
underlying mechanism (`bills.archived`) already existed and "Delete" was
already a soft archive — but `GET /api/bills` filtering archived bills out
server-side broke the History/Analytics bill-name lookup for exactly the
payments the owner wanted preserved, and archiving right after a final
payment left a dangling next-cycle pending payment forever.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 036  | Fix archived-bill history lookup, clean up dangling payments, relabel Delete as Archive | P1 | M | MED | — | DONE (`getBills()` in `server/storage.ts` no longer filters `archived`, so History/Analytics can resolve bill name/category for payments whose bill has since been archived; `deleteBill()` now wraps the archive in a transaction that also deletes that bill's not-yet-paid payments, closing the dangling-pending-payment gap; `dashboard.tsx`'s category dropdown and "no bills yet" empty state gained explicit `!b.archived` filters to keep their prior behavior now that the server returns archived bills too; the "Delete Bill" dialog is relabeled "Archive Bill" with copy describing the real effect (icon swapped `Trash2` → `Archive`), `onDeleteBill`/`useDeleteBill`/the DELETE route kept their names per scope; `history.tsx` now shows an "Archived" badge next to a payment's bill name when that bill is archived; drift check against `850ab7a` showed no changes to any in-scope file; `pnpm check` and `pnpm test` (3/3) both exit clean — the worktree's `node_modules` was missing most packages (only `typescript` present) and needed `pnpm install` before either would run correctly, unrelated to this plan's edits; live-DB verification against a running `pnpm dev` (this worktree's own `.env`/port 5050 config, copied from the main checkout) confirmed all Step 6 observations: created a test bill, marked it paid (confirming `resetPayment` created a next-cycle pending payment), archived it via the new dialog (copy read exactly as written, no more "remove its payment history"), confirmed the bill disappeared from the Dashboard active table and Upcoming, confirmed the dangling next-cycle pending payment was deleted while the paid payment survived, confirmed History showed the real bill name/category with an "Archived" badge, confirmed the category dropdown no longer offered the test bill's now-orphaned category, and cleaned up the test bill's row and payments directly from the dev DB afterward; committed as 3 commits on branch `advisor/036-archive-bill-history-and-copy`) |

Merged to `main` (fast-forward, `dd6f3d3`) and its worktree/branch cleaned
up same day, per owner confirmation.

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.

## Fifth pass — 2026-09-01

Single targeted plan (`plan <description>` mode, no full audit), same day
as the fourth pass — a follow-up UX observation from the owner after using
the app post-036: the Dashboard's bill tables render every not-yet-due bill
with the identical flat amber "Pending" badge regardless of how far out
it's due (an annual bill due 9+ months out reads exactly as urgent as one
due tomorrow). Investigation confirmed a single shared `BillTable`
component drives both bill tables, so the fix is a one-file, display-only
change — no new `status` value, filter, or schema change needed.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 037  | Tier the "Pending" status badge by due-date proximity | P2 | S | LOW | — | DONE (added `getUrgencyDisplay` helper hoisted to module scope in `client/src/pages/dashboard.tsx`, deriving badge label/color from `differenceInCalendarDays(item.dueDate, today)` for `"pending"` items — 0-3 days shows amber "Due Today"/"Due in Nd", 4-14 days shows new blue "Upcoming", 15+ days shows the muted `text-muted-foreground` "Scheduled" tier reused from the category badge; `paid`/`overdue` keep identical colors/logic, only their label capitalization moved from CSS `capitalize` into the string; `pnpm check` and `pnpm test` (3/3) both exit 0 — worktree's `node_modules` needed a full `pnpm install` first, unrelated to this change; `BillStatusItem`/`getStatus`/`statusFilter` untouched, confirmed via grep; verified against a live `pnpm dev` + real Neon DB: a temporary "Plan037 Test Bill" due in 2 days showed amber "Due in 2d", the existing "USI: Internet" bill due Sep 14 showed blue "Upcoming", all far-out annual bills (e.g. "Mint Mobile: Travis" due Jun 2027) showed muted "Scheduled", the Pending/Overdue filter pills and the "on track"/overdue count were unaffected by the new tiers, and the test bill was archived afterward to leave the DB clean; committed `f3e85c3` on branch `advisor/037-tier-pending-badge-by-due-date`, merged to `main` fast-forward as `696beec`, worktree/branch cleaned up. Superseded same-day by plan 038 — the owner tried it live and found the unbounded 15+-day "Scheduled" tier recreated the original flatness problem one threshold later, just further out.) |

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.

## Sixth pass — 2026-09-01

Single targeted plan (`plan <description>` mode, no full audit), same day
as the fifth pass. After 037 shipped, the owner found the day-count
tiering was the wrong axis entirely — annotated a live screenshot showing
bills 30-47 days out (monthly table) and bills 2-9 months out (annual
table) all still flattened under one "Scheduled" label. A design-critique
detour (via the `design:design-critique` skill) initially misdiagnosed
this as a visual-weight problem before the owner clarified the actual
model: what matters is whether a bill's next unpaid occurrence falls in
the *current* billing cycle (this calendar month for monthly bills, this
calendar year for yearly bills) or a future one — not how many days away
it is. Plan 038 replaces 037's day-count tiers with that cycle-membership
check.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 038  | Replace day-count status tiers with billing-cycle membership | P1 | S | LOW | — | DONE (rewrote `getUrgencyDisplay` in `client/src/pages/dashboard.tsx` to key the `"pending"` case on billing-cycle membership instead of `differenceInCalendarDays` — monthly bills check `isSameMonth`+`isSameYear` against today, yearly bills check `isSameYear`; unpaid-and-due-this-cycle shows amber "Due" (037's reused near-term amber), unpaid-and-due-a-future-cycle shows muted "Next Cycle" (037's reused far-tier muted style); `paid`/`overdue` branches and the now-unused `differenceInCalendarDays` import left untouched/pruned per plan, byte-for-byte identical colors to 037; drift check against `696beec` was clean; `pnpm check` exits 0 (one pre-existing unrelated `analytics.tsx` `Set<string>` iteration error surfaced before `pnpm install`, gone after — worktree's `node_modules` was stale, same class of issue as plans 036/037); `pnpm test` 3/3 pass; all Done-criteria greps pass (`differenceInCalendarDays`/`"Due Today"`/`"Scheduled"` → 0 matches, `"Next Cycle"` → 1 match, `statusFilter` line unchanged, only `dashboard.tsx` modified); live-DB verification against a running `pnpm dev` + real Neon DB using the owner's existing bills (no test data created, per plan): `USI: Internet`/`Integrity: Vehicle Insurance` (due later this September) showed amber "Due"; `RCU: Mortgage` (due Oct 1) and the rest of the monthly table (Oct 8/11/17) showed muted "Next Cycle"; `Integrity: Home Insurance`/`Claude`/`Qobuz`/`Figma` (due later in 2026) showed "Due"; `Mint Mobile: Travis`/`Mint Mobile: Erin` (due Jun 2027) showed "Next Cycle"; inspected the live DOM className directly and confirmed both badges' Tailwind classes match the plan's spec exactly; the Pending filter pill showed all 12 unpaid bills mixing both tiers together (filter logic unaffected), the Overdue filter pill correctly showed 0 results (no overdue bills exist right now); paid/overdue badge colors could not be live-verified — the dev DB currently has 0 paid and 0 overdue bills (0% Paid) — but that branch is byte-for-byte unchanged from 037's already-verified code, confirmed via diff; committed `1efeb7d` on branch `advisor/038-cycle-based-due-status`, not pushed) |

Re-run `/improve` against this repo in the future to catch anything new
that's landed since this pass.
